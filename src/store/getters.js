// 获取最终的状态信息
// export const resturantName = (state) => state.resturantName;

export const adminUser = (state) => state.adminUser;

export const student = (state) => state.student;

export const seatList = (state) => state.seatList;
